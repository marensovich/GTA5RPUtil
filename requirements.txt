# Установка: .venv\Scripts\pip install -r requirements.txt
#
# ВАЖНО: pytesseract — это только Python-обёртка. Саму программу
# Tesseract-OCR при разработке можно установить отдельно (см. README.md),
# либо просто пользоваться уже установленной — при сборке в .exe она
# встраивается внутрь и отдельная установка на других машинах не нужна.
#
# opencv-python сознательно НЕ используется — раньше был нужен только
# pyautogui/pyscreeze для поиска шаблонов на экране (confidence-матчинг),
# но это ~85 МБ веса в exe. Вместо него — element_matcher.py (та же
# нормализованная кросс-корреляция, только на numpy). Не добавляйте
# opencv-python обратно без необходимости — сборка сразу вырастет.
#
# PySide6 — GUI (Qt): сознательный выбор в пользу размера exe (было
# ~53 МБ на Tkinter, будет заметно больше на Qt) ради нормальных
# градиентов/теней/анимаций через QSS вместо PIL-костылей.
PySide6>=6.5.0
pyautogui>=0.9.54
pyscreeze>=1.0.1
Pillow>=10.0.0
numpy>=1.24.0
psutil>=5.9.0
pytesseract>=0.3.10
pywin32>=306; sys_platform == "win32"
keyboard>=0.13.5; sys_platform == "win32"  # глобальная горячая клавиша аварийной остановки (F3 по умолчанию)

# Только для сборки .exe (не нужен для обычного запуска через python main.py)
pyinstaller>=6.0.0
